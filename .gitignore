# Dependencies
node_modules/
.data/

# Local agent/tooling environment (opencode assistant config, plugins, MCP) —
# not product source; contains machine-specific paths and internal IDs.
.opencode/
opencode.json

# Local agent wallets (contain seed phrases) — never commit
wallet-testnet.json
agent-kasap-wallets.json

# Rust build artifacts (standalone oracle at oracle/, and silverscript submodule)
target/
**/target/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
.playwright/

# Env / secrets
.env
.env.*
